package com.example.demo.service;

import com.example.demo.controller.dto.JoinRequest;

public interface TestService {
    String join(JoinRequest joinRequest);
//    String searchAll(JoinRequest joinRequest);
}
