spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://10.1.0.239:3306/spring
    username: spring
    password: P@ssw0rd

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true

  thymeleaf:
    prefix: classpath:templates/
    check-template-location: true


logging:
  level:
    org.hibernate.sql: debug
    org.hibernate.type: trace